{
  "name": "pandcasa-cr",
  "private": true,
  "type": "module",
  "scripts": {
    "css:build": "npx tailwindcss -i css/styles.css -o dist/tailwind.css --minify",
    "css:watch": "npx tailwindcss -i css/styles.css -o dist/tailwind.css --watch"
  },
  "dependencies": {
    "@vercel/functions": "^3.4.3"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
